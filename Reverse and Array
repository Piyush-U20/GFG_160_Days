#Reverse an Array
class Solution {
  public:
    void reverseArray(vector<int> &arr) {
        // code here
        vector<int> temp;
        for(int i=arr.size()-1;i>=0;i--)
        {
            temp.push_back(arr[i]);
        }
        arr=temp;
    }
};
